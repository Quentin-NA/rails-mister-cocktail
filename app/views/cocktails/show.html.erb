<h1><%= @cocktail.name %></h1>
<% @cocktail.doses.each do |dose|%>
<tbody>
  <tr>
    <td><p>Description: <%= dose.description %></p></td>
    <td><p>Ingrédients: <%= dose.ingredient.name %></p></td>
    <td><%= link_to "Delete dose", dose_path(dose), method: :delete %></td>
  </tr>
</tbody>
    <% end %>
<p>
<%= link_to "Add dose", new_cocktail_dose_path(@cocktail) %> |  | 
<%= link_to "Home", root_path %></p>
